kind: Operation
category: math
op: math.outer
name: Outer Product
label: Outer Product
description: Computes the outer product of two vectors — W[i,j] = a[i] * b[j].
initial_width: 200
inputs:
  - name: a
    type: tensor
    description: First vector
  - name: b
    type: tensor
    description: Second vector
outputs:
  - name: W
    type: tensor
    description: Outer product matrix
